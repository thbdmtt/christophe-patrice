name: Deploy to GitHub Pages

# GitHub Pages n’est plus l’hôte du site (Cloudflare Pages, accès par mot de passe).
# Ce workflow est conservé désactivé pour ne pas republier une URL publique.
on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Déploiement GitHub Pages désactivé. Publier avec wrangler pages deploy."
